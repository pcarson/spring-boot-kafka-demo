spring:
  application:
    name: rest-jpa-metrics-demo

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      auto-offset-reset: earliest
      group-id: user
test:
  topic: user

kafka:
  user:
    topic: "user"
    group:
      id: "user"

metrics:
  environment: local

